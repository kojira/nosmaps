{
  "name": "nosmaps",
  "private": true,
  "version": "0.0.0",
  "description": "Relay-native catalog of Nostr tools. Static site, no backend.",
  "scripts": {
    "build": "esbuild build/entry-rx-nostr.js --bundle --format=esm --platform=browser --outfile=dist/rx-nostr.js && esbuild build/entry-rx-nostr-crypto.js --bundle --format=esm --platform=browser --outfile=dist/rx-nostr-crypto.js && esbuild src/entry/landing.ts --bundle --format=esm --platform=browser --outfile=dist/landing.js && esbuild src/entry/nip-explorer.ts --bundle --format=esm --platform=browser --outfile=dist/nip-explorer.js",
    "data": "node tools/build-data.mjs",
    "verify-catalogue": "node tools/verify-catalogue.mjs && node tools/check-descriptions.mjs",
    "check-descriptions": "node tools/check-descriptions.mjs",
    "test": "playwright test",
    "typecheck": "tsc --noEmit && node tools/check-layering.mjs",
    "check-layering": "node tools/check-layering.mjs"
  },
  "dependencies": {
    "@rx-nostr/crypto": "3.1.6",
    "rx-nostr": "3.7.5"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "esbuild": "0.28.1",
    "typescript": "^7.0.2"
  }
}
