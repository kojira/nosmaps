# Independent design review 1 — response matrix

Status: resolved — 18/18
Reviewed document: `repos/nosmaps/design-relay-native-data.md`
Source review: `repos/nosmaps/design-relay-native-review-1.md`
Revision date: 2026-08-16

| ID | Severity | Review issue | Status | Revised section | Resolution and reason | Verification |
|---|---|---|---|---|---|---|
| P0-1 | P0 | Cold-start manifest completeness | resolved | §§0, 3, 13, 15 | Deleted relay-internal shards/generation-index discovery. One stable addressable pointer commits to one RFC-8785 canonical, SHA-256-addressed Blossom blob. Pointer winner is fetched with one REQ/relay; HTTP mirror fallback independently verifies bytes/hash/schema/count. Verified prior generation is stale fallback; unverified bytes are never truth. This makes completeness a signed whole-blob property rather than inference from relay `limit`. | Bad hash/size/count/noncanonical/missing mirror/all-mirror failure fixtures; pointer trace `1 REQ/relay`; HTTP attempt bound; stale/no-cache states. |
| P0-2 | P0 | Deletion reconstruction after cache wipe | resolved | §§0, 4, 15 | Retracted impossible global deletion/strong-consistency guarantee. Catalog state is reconstructed from latest observed verified curator snapshot plus observed addressable `active|withdrawn` winners. Kind 5 is best-effort cleanup/privacy only. Exact AP guarantee, freshness/coverage diagnostics, cache-wipe sequence, invariants, partitions, and eventual convergence are specified. | Empty-cache rebuild without historical kind 5; partitions A–C; old-record-survives relay; eventual convergence and arrival-order property tests. |
| P1-1 | P1 | No current NIP-11 `max_filters`; byte limits missing | resolved | §§11.1, 13, 15 | `MAX_FILTERS_PER_REQ` is explicitly app-owned. Full serialized UTF-8 REQ byte packing uses `max_message_length` or fallback, plus `max_subscriptions`, `max_limit`, `max_subid_length`, event/content/tag and array caps. Physical budget formula accounts for chunks. | Boundary fixtures for bytes, arrays, subid, limit, absent/malformed NIP-11; captured physical REQ bounds. |
| P1-2 | P1 | rx-nostr/NIP-67 completion mismatch | resolved | §§0, 8.1, 14 | MVP is NIP-67-independent. Winner-before-page overfetch tracks per-relay same-timestamp boundary ids, caps saturation as incomplete, and never uses an empty page as completeness proof. Raw EOSE correlation is future work only. | Timeout/empty/boundary-saturation/replacement fixtures; assert no `finish/more` dependency. |
| P1-3 | P1 | Fixed overlap misses reconnect gaps | resolved | §§9.2, 13, 15 | No fixed overlap. Every disconnect/reconnect/recovery/resume/scope change triggers mandatory Backward gap query with per-relay/scope `(created_at,boundaryIds)`, merge/dedupe, then Forward restart. Budget charges each affected relay/scope. | Outbound traces and high-water assertions for all triggers, timeout/incomplete, broadened scope. |
| P1-4 | P1 | Missing screen dependencies / Cartesian filters | resolved | §§9.1, 13, 15 | Defines screen/tab dependency graph, cache prerequisites, lazy dependencies, exact event ids, author-grouped `#d` filters, coalescing, and zero-before-open behavior. Removes authors×`#d` Cartesian starvation. | Serialized REQ snapshots per scenario; exact logical counts; lazy/coalescing/no-N+1 assertions. |
| P1-5 | P1 | Multi-curator merge undefined | resolved | §§4.4, 5 | Curator-local pointer/blob validation precedes a pure sorted merge. One active trusted curator can list; recommendation count is separately reduced from `30267` winners. Field provenance, active/withdrawn conflict, stale/quarantine, key removal/rotation, eviction, and partitions are deterministic. | Arrival-order permutation, curator conflict, stale partition, recommendation, key removal/rotation fixtures. |
| P1-6 | P1 | Unsafe supersedes authority | resolved | §6 | Only a newer winner at the old `32267` coordinate signed by the same publisher is authoritative. Curator/`30371` mappings are scoped evidence. Depth, cycles, conflicts, deletion, missing destination, and UI diagnostics are explicit. | Migration chain/depth/cycle/deleted-authority/conflict/partition fixtures. |
| P1-7 | P1 | Bookmark lost updates/plaintext | resolved | §10, §13 | Collects cross-relay `10003` candidates, applies NIP-01 tie rule, performs pre-sign reread, merge/retry, monotonic bounded timestamp, per-relay OK/read-back, and post-publish winner check. Admits irreducible simultaneous-edit limit. Plaintext logging is forbidden and best-effort zeroing/drop is specified. | Concurrent-device/same-second/retry/read-back/upper-limit/decrypt/log-redaction tests. |
| P2-1 | P2 | Publisher identity/official claim underspecified | resolved | §2.2 | MVP is curator-selected publisher claim, never legal/official ownership. Closed claim levels distinguish publisher signature, NIP-05 linkage, curator selection, and evidence. Duplicate coordinates remain distinct; future delegation requires a new signed contract. | Claim-level validation/UI fixtures; duplicate-coordinate non-merge test. |
| P2-2 | P2 | Candidate-kind schemas incomplete | resolved | §7 | Adds normative common and per-kind `30367`–`30372` contracts: required fields/tags, enums, unknown behavior, limits, `d` grammar, JCS/hash inputs, feature namespace, environment hash, evidence subject grammar, and registry-collision migration. | Schema/canonicalization/hash/limits/unknown-major/collision conformance fixtures. |
| P2-3 | P2 | Review winner/page ordering absent | resolved | §8.1 | Raw overfetch → validation/deletion → addressable winner reduction → withdrawn exclusion → deterministic unique page. Inclusive boundary expansion and incomplete caps prevent same-second skips/loops. | Duplicate versions, one-user/tool, same timestamp, withdrawn/deleted, replacement and timeout fixtures. |
| P2-4 | P2 | Reaction aggregation undefined | resolved | §8.2 | Latest non-deleted event per `(pubkey,target event id)` with lowest-id tie; `+`, `-`, empty, emoji, other normalization; newer reaction replaces prior bucket; old replacement ids stay historical. | Multi-reaction/delete/tie/emoji/old-target aggregation fixtures. |
| P2-5 | P2 | Future timestamp reevaluation undefined | resolved | §8.3 | Persists receipt/source/relay limit metadata in cache, uses fixed eligibility against receipt time and relay upper limit, sets 30-day horizon, enumerates reevaluation triggers, exposes clock skew, and requires frozen-time tests. | Frozen receipt/wall-clock, timer/foreground/clock-change/relay-info, horizon and winner tests. |
| P2-6 | P2 | NIP-22 examples/behavior insufficient | resolved | §12, §14 | Explicitly future draft/optional and MVP-excluded. Gives top-level and nested `A/a/E/e/K/k/P/p` examples, current review winner context, `#A` probe/fallback, replacement/deletion treatment. | JSON tag fixtures and uppercase generic-tag indexing live-probe gate. |
| P2-7 | P2 | Relay-dependent publishability unresolved | resolved | §§3.5, 11.2, 13 | Separates Blossom blob size/upload policy from small relay pointer limits. Requires target-server upload/mirror descriptor validation, HEAD/GET/hash visibility and Blossom quorum, then relay OK/read-back quorum with per-target failures. | Blossom 413/auth/payment/mirror/descriptor/hash probes; pointer message-limit and relay visibility fixtures. |
| P3-1 | P3 | Source inventory inconsistent | resolved | §14 | Adds NIP-22 and NIP-45 with explicit adoption decisions, plus primary Blossom BUD-01/02/04/06/11 and commit fixation. NIP-45 count is rejected as completeness proof. | Static source/adoption grep and primary-source review. |
| P3-2 | P3 | Pseudocode overclaims compile verification | resolved | §§0, 3.4, 9.3, 15 | Every TS snippet is labeled non-compiling pseudocode. Implementation acceptance requires exact lockfile `rx-nostr@3.7.5`, `@rx-nostr/crypto@3.1.6`, DOM/NIP-07 augmentation, `tsc --noEmit`, and project build. | Later implementation-phase lockfile fixture/typecheck/build; no compile claim in this design. |

## Completion gate

- [x] All 18 review findings are read and mapped to a `resolved` row.
- [x] Relay-internal manifest shards/generation index and reconstructable deletion-ledger strong guarantee are absent.
- [x] Blossom pointer/blob JSON, mirror fallback, fetch/verify pseudocode, publication probes, AP invariants and partition examples are present.
- [x] Nostr and Blossom HTTP budgets are separate and byte/array chunk growth is bounded.
- [x] Closed relay hostnames are absent; both default relay candidates are present.
- [x] Relay/Blossom capabilities are implementation-preflight unknowns, not assumptions.
- [x] `git diff --check` passes.
- [x] No code implementation, dependency addition, commit, push, or Pages publication was performed.
